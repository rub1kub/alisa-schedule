# Проверки

## Тексты инструкции — 21 сентября 2026

По [Sepia v0.12.0](https://github.com/Nanako0129/sepia/blob/main/skills/sepia/SKILL.md) отредактированы пояснения на `/guide` и в `docs/USER_GUIDE.md`: удалено вводное перечисление, упрощены формулировки о группе, настройках и выходе. Все фразы в кавычках сохранены дословно; добавления и замены проверены на избыточность.

Прошли 8 тестов публичных страниц, проверка сохранности команд и `git diff --check`. После публикации GET главной и инструкции, HEAD инструкции вернули 200; HTML инструкции совпал с локальным исходником. В браузере проверены новый текст и раскрытие настроек. Образ прошёл валидацию данных, контейнер запущен через `docker compose up -d --wait`. Резервная копия — `/opt/alisa-schedule/before-sepia-copy-20260921`, образ — `alisa-schedule:before-sepia-copy-20260921`. Хеши главной, переключения мини-приложения и проверяемых файлов бота и Apache не изменились.

## Упрощение главной и первый кадр мини-приложения — 21 сентября 2026

Главная сведена к названию, подписи «Расписание колледжа в Алисе», одной команде и ссылке на инструкцию. Бот, API, исходники, поддержка и автор перенесены в компактный нижний ряд. Блок «О проекте» и длинные описания удалены с главной; подробные примеры остаются на `/guide`.

Причина вспышки: выбор экрана выполнялся отложенным `kepik-entry.js` после загрузки Telegram SDK, когда HTML лендинга уже мог отобразиться. Теперь синхронный скрипт в `<head>` проверяет `tgWebAppData` в хеше и сохранённых параметрах SDK. При наличии данных CSS скрывает лендинг до первого кадра. Окончательный выбор по `Telegram.WebApp.initData` и серверная проверка подписи сохранены. Заблокированное хранилище и повреждённый кэш не скрывают обычный сайт. При недоступном SDK на запуске Telegram остаётся экран ошибки приложения.

Прошли 27 тестов и Ruff. В браузерном стенде с настоящим SDK и задержкой его загрузки на 3 секунды зафиксировано 0 кадров с видимым лендингом: 182 кадра ожидания при первом входе и 181 при возврате с данными из sessionStorage. Обычный вход без данных: 0 кадров ожидания, лендинг виден, мини-приложение скрыто. Это проверка выбора экрана, без авторизации реального пользователя Telegram.

Опубликованы HTML главной и обновлённый выбор экрана. Резервная копия — `/opt/alisa-schedule/before-minimal-alice-20260921`, образ — `alisa-schedule:before-minimal-alice-20260921`. Проверены 12 внешних GET/HEAD-запросов к главной, `/about` и `/guide` по HTTP/HTTPS; оба webhook отвечают корректно. На опубликованной главной при ширине 390 px горизонтального переполнения нет.

## Ссылки проекта и инструкция по Алисе — 21 сентября 2026

Владелец подтвердил публикацию навыка. Удалены текущие пометки об ожидании модерации в карточке проекта и пользовательской документации. История предыдущих отправок сохранена.

На главной и `/about` добавлены ссылки на Telegram-бота, документацию API, исходники навыка, поддержку и отдельную страницу `/guide`. Текст написан от лица одного автора («мой проект», «мой Telegram-бот»). Сохранены тёмный фон, крупный заголовок и красные, синие, зелёные акценты. Инструкция включает выбор группы, быстрые запросы, уточнения, форматы, чужие группы и преподавателей, сценарии «Дом с Алисой». Разделы раскрываются без JavaScript.

Проверены 14 тестов страниц и переключения мини-приложения, Ruff, валидация данных внутри нового образа. Десктопный вид обеих страниц проверен в браузере; при ширине 390 px инструкция не имеет горизонтального переполнения, раздел сценариев раскрывается. На публичном сайте проверен переход с главной к инструкции. Навигация существующего мини-приложения добавляет пустой параметр `tgWebAppData` к внутренним ссылкам вне Telegram; он не мешает открытию инструкции.

Развёрнуты страницы, маршрут инструкции и независимая конфигурация Apache. Обновлён только публичный фрагмент `templates/kepik_landing.html` в существующем веб-приложении; его JavaScript, исходный шаблон, авторизация, API и бот не менялись. Резервная копия: `/opt/alisa-schedule/before-guide-20260921`; предыдущий образ: `alisa-schedule:before-guide-20260921`. Контейнер healthy, Apache Syntax OK.

Внешние ссылки (бот, API, исходники, поддержка) вернули HTTP 200. Публичный и приватный webhook вернули корректное приветствие для синтетического нового пользователя без группы. Внешние HTTPS-главная и инструкция, HTTP `/about` и `/guide` — 200. Две попытки полного скрипта через urllib остановились на HTTP-тайм-ауте; при отдельной проверке HTTP-главной через httpx GET и HEAD вернули 200 за 0,41 и 0,2 с, на сервере все три маршрута также вернули 200, релевантных ошибок Apache нет. Причина тайм-аутов не установлена; полный набор из 32 проверок не заявляется как пройденный.

## Повторная отправка — 18 сентября 2026

По запросу владельца сайт бренда в карточке изменён на `https://kkepik.rub1kub.ru/` и сохранён. Навык повторно отправлен с комментарием о публичном лендинге на главной, отсутствии требований Telegram и авторизации, 20 успешных HTTP-проверках, подтверждении сайта в Вебмастере и сценарии проверки навыка. Запрошены повторная проверка названия «Кэпик» и конкретные сведения об ошибке, если сайт снова не откроется. Консоль подтвердила статус «на модерации». Одобрение и публикация пока не подтверждены.

## Лендинг на главной — 18 сентября 2026

По прямому запросу владельца изменена точка входа существующего мини-приложения: без Telegram `initData` корень домена показывает лендинг; при непустой строке `Telegram.WebApp.initData` показывает прежний интерфейс. Проверка подписи и API не менялись. `initDataUnsafe.user` без `initData` не включает мини-приложение. Разметка лендинга присутствует в исходном HTML и видна без JavaScript; стили изолированы, стили мини-приложения включаются только в его режиме. Переадресаций, новых параметров URL и отдельных ключей кэша нет.

Изменён только `templates/index.html`, добавлены `templates/kepik_landing.html` и `static/js/kepik-entry.js` в `/var/www/kkepik.ru`. Исходный шаблон сохранён в `/opt/alisa-schedule/root-landing-backup-20260918/index.html`. Перезагружен WSGI-процесс через изменение времени `app.wsgi`, без изменения его содержимого. Код API, БД, авторизация, бот и Apache-конфигурация не менялись. Генератор интеграции — `scripts/prepare_root_landing.py`, исходник выбора экрана — `deploy/miniapp/entry.js`.

Прошли 11 тестов страницы и переключения, Ruff, Node syntax check. В браузере главная показывает лендинг; при тестовых неподписанных `initData` выбран интерфейс приложения, сервер показывает «Требуется авторизация Telegram». Горизонтального переполнения при 390 px нет. Авторизованный сценарий с настоящим аккаунтом Telegram в этой проверке не воспроизводился. `/about` остаётся доступным. Заявка на модерацию повторно не отправлялась.

## Повторный отказ и диагностика — 18 сентября 2026

Модератор повторно сообщил, что сайт не загружается; название «Кэпик» и имя «Кэпика» не приняты. В карточке по-прежнему указан точный URL `https://kkepik.rub1kub.ru/about`.

При текущей проверке `/about`, `/about/` по HTTPS и `/about` по HTTP вернули 200. Лендинг после перезагрузки отображается в браузере. Сертификат действителен до 30 октября 2026; контейнер healthy, перезапусков нет. В текущих и предыдущих журналах Apache за 18 сентября найдено шесть GET/HEAD-запросов к `/about` (пять HTTPS, один HTTP), все 200. Они включают наши проверки и не идентифицируют модератора. Последние обнаруженные 403 относятся к 17 сентября до исправления маршрутов. Ошибки DNS/TLS/сетевого подключения могут не попадать в access log.

Главная страница домена показывает «Откройте приложение из Telegram». Возможно, проверялся корень домена; подтверждения этому нет. Публичная страница также запрещает встраивание через `frame-ancestors 'none'`; использование iframe модератором неизвестно. Ограничение не меняли без доказанной необходимости. Одно SSH-подключение не прошло этап banner exchange за 10 секунд; следующая попытка успешна. Это не доказывает сбой HTTPS.

Сайт, бот, backend и настройки Яндекса не изменялись. Повторная заявка не отправлялась. Для установления причины нужен точный URL и вид ошибки со стороны проверяющего; заготовка обращения находится в `MODERATION_REPLY.md`.

## Минималистичный лендинг — 17 сентября 2026

По запросу владельца страница упрощена в стиле `rub1kub.ru`: тёмный фон, название по центру, короткая подпись и ссылки с красным, синим и зелёным акцентами. Информация о колледже, разработчике, боте и статусе навыка сохранена в раскрываемом блоке «О проекте». Макет разговора, карточки и длинные инструкции удалены.

Проверены 5 тестов страницы, 16 публичных вариантов адреса, раскрытие информации и отсутствие горизонтального переполнения при ширине 390 CSS-пикселей. Опубликованный HTML совпадает с исходником. Контейнер healthy. Сохранены образ `alisa-schedule:before-minimal-20260917` и файл `/opt/alisa-schedule/before-minimal-20260917.html`. Настройки и заявка навыка не менялись.

## Доступность страницы и приветствие — 17 сентября 2026

После следующего отказа модерации проверены варианты адреса. Точный `https://kkepik.rub1kub.ru/about` работал, но `/about/` и HTTP возвращали 403 из-за слишком строгих условий в Apache. Исправлены эти ограничения только для публичного лендинга; добавлен маршрут со слешем в FastAPI. Webhook и файл Вебмастера сохраняют прежние ограничения. Нельзя установить, какой именно адрес открыл модератор, поэтому обнаруженная ошибка не выдаётся за доказанную причину его отказа.

Через публичный адрес прошли 16 проверок HTTP/HTTPS, GET/HEAD, со слешем и параметрами запроса. В Яндекс Вебмастере основной робот Яндекса получил для `https://kkepik.rub1kub.ru/about/` ответ 200 OK, UTF-8, 10,11 КБ; время ответа составило 1205 мс. Для повторения внешних проверок добавлен `scripts/check_landing.py`.

Первый запуск без группы теперь объясняет назначение навыка и предлагает короткий номер группы; при сохранённой группе — вопрос «что завтра» или день недели. Тексты `welcome_group` и `welcome` остаются настраиваемыми. Прямой запрос расписания не получает лишнего вступления и сохраняет исходный день. Прошли 85 релевантных тестов, Ruff и валидация данных. Через HTTPS оба ID проверены с группой и без неё: назначение и следующая команда звучат, разговор остаётся открытым, настройки не перезаписываются.

В карточке оставлены «Кэпик» и склонение «Кэпика»; оба отклонённых расширенных имени удалены. В повторную заявку включены результат проверки Яндекса, исправленные приветствия и просьба перепроверить имя проекта после исправления сайта. Консоль подтвердила статус «на модерации». Признание однословного имени брендом остаётся решением модератора.

Общий backend обновлён с резервной копией `alisa-schedule:before-welcome-20260917` и `/opt/alisa-schedule/before-welcome-20260917.tar.gz`. При откате код и `data/responses.json` восстанавливаются вместе.

## Лендинг и бренд «Кэпик» — 17 сентября 2026

Опубликована общедоступная страница `https://kkepik.rub1kub.ru/about`: описание проекта, связь с Telegram-ботом, примеры возможностей и ссылки на разработчика и исходники. Тексты подготовлены по Sepia. Страница честно указывает, что навык готовится к публикации; расписание в примере вымышлено.

Поддомен добавлен в Яндекс Вебмастер и подтверждён публичным HTML-файлом: в интерфейсе проверена роль «Владелец». В Диалогах сохранены имя «Кэпик», варианты «Кэпика», «Расписание Кэпика», «Расписания Кэпика», новый текст и адрес лендинга для верификации бренда. Карточка отправлена повторно с пояснением исправлений. Консоль подтвердила «на модерации»; это ещё не одобрение и не публикация.

Лендинг работает через отдельную конфигурацию Apache и контейнер навыка. Существующие файлы бота и virtual host сверены по контрольным суммам и не изменились. Главная страница и документация API отвечают HTTP 200. Проверочный HTML-файл доступен, POST на лендинг запрещён. Обе версии навыка через HTTPS вернули разные ответы на «помощь» и «что ты умеешь», `/readyz` — ready.

Первый набор проверки страницы и справки: 16 тестов. После добавления файла Вебмастера прошли 7 тестов страницы и допустимых ID навыка. Ruff и Apache configtest прошли. Сохранены предыдущий образ `alisa-schedule:before-brand-20260917` и архив `/opt/alisa-schedule/before-brand-20260917.tar.gz`. Новых зависимостей нет.

Лендинг осмотрен в браузере на широком экране и в эмуляции шириной 390 CSS-пикселей. Горизонтального переполнения нет. Временная эмуляция отключена после проверки.

## Вспомогательные команды — 17 сентября 2026

После повторной отправки «Расписание Кэпика» отклонено: остаются замечания к названию, активационным именам и подтверждению бренда. Карточка не переименована и повторно не отправлена. Статус в файле карточки исправлен на `rejected` по сообщению модератора, которое передал владелец.

«Что ты умеешь» теперь кратко описывает возможности, «помощь» перечисляет конкретные команды и способ выхода. Явный вопрос о возможностях распознаётся отдельно даже при метке `YANDEX.HELP`. Оба текста настраиваются в `data/responses.json`; одинаковые значения отклоняются валидатором. Справка сохраняет незавершённый выбор группы, преподавателя, дня, номера пары и продолжение длинного ответа.

Прошли 89 релевантных тестов в `test_help.py`, `test_language.py`, `test_dialog.py`, `test_responses.py` и `test_auto_exit.py`. Проверены разные вспомогательные ответы, кнопки, сохранение исходного вопроса и выбранного дня, режим выхода. Ruff и валидация данных прошли; остаются два прежних предупреждения тестового клиента. Зависимости, публичный протокол и хранение настроек не менялись.

Код развёрнут в общем backend. Через HTTPS оба ID прошли по 10 запросов: отдельная справка, возможности с меткой `YANDEX.HELP`, сохранение вопроса при выборе группы, кнопка «Завтра», открытый разговор и выход. Использовались синтетические состояния; настройки владельца не изменялись. Контейнер healthy, перезапусков нет, `/readyz` — ready. Развёрнутые файлы совпали с локальными; контрольные суммы проверенных файлов бота и virtual host не изменились.

Перед обновлением сохранены образ `alisa-schedule:before-help-20260917-1518` и архив `/opt/alisa-schedule/before-help-20260917-1518.tar.gz`. При откате код и `data/responses.json` нужно восстановить вместе. Распознавание имени на физической Станции и принятие бренда модератором этими проверками не подтверждаются.

## Повторная модерация — 17 сентября 2026

Модератор отклонил однословные название и активационные имена, а также указал на аббревиатуру без расшифровки. Владелец выбрал «Расписание Кэпика». Название, три дополнительных имени, пример запуска и описание сохранены в Яндекс Диалогах. В описании раскрыто название колледжа и объяснено произношение названия программы-источника. Все активационные имена состоят из двух слов.

Карточка отправлена повторно с пояснением исправлений и сценарием проверки. Консоль подтвердила статус «на модерации». Это подтверждение отправки, а не одобрения или публикации; новые голосовые команды ещё не проверены на Станции. Приватная тестовая версия сохраняет прежние имена.

Проверены JSON карточки, число слов в именах, отсутствие отклонённых однословных вариантов и согласованность актуальных инструкций. Изменены только карточка и документация; тесты backend для этих правок не запускались. Требования сверены с [документацией Яндекса](https://yandex.ru/dev/dialogs/alice/doc/ru/publish-settings) и [действующими требованиями](https://yandex.ru/legal/dialogues_requirements/ru/).

## Разговор после отдельного запуска — 16 сентября 2026

Отдельный запуск без вопроса теперь оставляет навык открытым для уточнений. Запуск сразу с вопросом сохраняет автовыход после полного ответа. Режим разговора хранится только в сессии и не записывается в личные настройки при выборе группы. Явное предпочтение выхода имеет приоритет. «Спасибо», «всё», «всё, спасибо» и «спасибо, всё» завершают разговор; благодарность вместе с вопросом его не прерывает.

Прошли 142 теста диалога, ответов, состояний и адресатов. После добавления выхода по благодарности прошёл набор из 130 тестов языка, уточнений и разговорных запросов; наборы пересекаются. Проверены первый выбор группы, гостевой и авторизованный пользователь, сохранение дня и номера пары, новый запуск со старым состоянием, явные настройки выхода, отключение нового поведения в конфигурации и сбой источника. Lint, форматирование и валидация данных прошли; новых зависимостей нет.

После развёртывания каждый из двух ID навыка прошёл восемь HTTPS-запросов: отдельный запуск → завтра → предметы → вторая пара → спасибо; новый запрос одной фразой; отдельный запуск с принудительным автовыходом → завтра. Ответы совпали между версиями, предметы сверены с API для нужной даты. Проверки использовали отдельные синтетические состояния и не записывали пользовательские настройки.

В чате опубликованной приватной версии отдельно проверено: запуск → «завтра» → «какие предметы завтра» → «спасибо». День сохранился, после двух ответов сессия оставалась открытой, после благодарности консоль показала её завершение. Личные настройки не менялись. На физической Станции новый порядок завершения пока не проверен.

Контейнер healthy, перезапусков нет, `/readyz` — ready. Файлы на сервере совпадают с локальными; проверенные файлы бота и virtual host не изменены. Сохранены образ `alisa-schedule:before-launch-modes-20260916` и архив `/opt/alisa-schedule/before-launch-modes-20260916.tar.gz`. При откате восстановить код и `data/responses.json` вместе.

В опубликованных настройках приватной версии подтверждено имя «Кэпик тест». Сообщение Станции «такого навыка нет» этим изменением не исправлено: backend не управляет распознаванием активационной команды. Проверка в консоли Яндекса не заменяет голосовой запуск на Станции.

## Разговорные запросы — 16 сентября 2026

Три агента проверили 152 реплики и шага в ролях студента с короткими запросами, студента с подробными вопросами и собеседника с уточнениями. Найденные ошибки исправлены и описаны в [CONVERSATION_QA.md](CONVERSATION_QA.md). Два агента независимо повторили свои сценарии; дополнительные замечания также получили исправления и регрессионные тесты.

В релевантном наборе прошли 243 теста, включая 70 новых в `test_student_queries.py`. После последней правки разбора преподавателя повторно прошли 175 затронутых тестов. Проверены адресаты, номера и количество пар, конфликтующие даты, короткие команды, отрицания в выборе формата, начало и конец занятий, продолжение уточнений и сохранение личных настроек. Lint, форматирование, валидация данных и сборка контейнера прошли. Новых зависимостей нет; сохраняются два прежних предупреждения тестового клиента.

Через HTTPS оба настоящих ID прошли по 18 запросов: ответы совпали, полные ответы закрывали сессию при включённом автовыходе, разовые вопросы не записывали настройки. Использовались синтетические состояния; хранилище владельца в Яндексе не изменялось. После финального уточнения разбора преподавателя этот маршрут отдельно перепроверен для обоих ID.

Код развёрнут в общем backend двух навыков. Контейнер healthy, `/readyz` — ready. SHA-256 развёрнутых файлов сверены с локальными. Контрольные суммы проверенных файлов бота и virtual host не изменились; существующие сайт и документация API отвечают HTTP 200. Резервная копия: образ `alisa-schedule:before-student-queries-20260916` и `/opt/alisa-schedule/before-student-queries-20260916.tar.gz`. Код и `data/responses.json` откатываются вместе.

Приватная карточка опубликована с именами «Кэпик тест», «Кэпика тест», «ККЭПика тест» и голосом «Алиса». Публичная карточка на момент проверки остаётся на модерации; её настройки не менялись. Улучшения backend уже общие, доступ публичного навыка на Станции зависит от публикации. Физическое распознавание новых фраз и звучание на Станции не проверены.

## Уточнение предметов — 16 сентября 2026

Добавлены запросы конкретного номера пары и разовые уточнения предметов. «Предметы» больше не меняет постоянный режим; для этого остаётся «называй предметы». В открытом разговоре уточнение сохраняет день и группу или преподавателя. После первой пары или конкретного номера короткое уточнение относится к этой паре. После выхода требуется новый запуск с нужным днём.

Прошли 168 релевантных тестов, включая 37 новых. Проверены разные предметы одного преподавателя, распознавание номера словами и цифрами, первая фактическая пара и точный номер 1, подгруппы, разовая другая группа, уточнение неверного номера и даты, отсутствие публикации, неизвестный предмет преподавателя, продолжение длинного ответа и сохранение пользовательского режима. Lint, форматирование и валидация данных прошли.

После сборки и развёртывания оба ID навыка прошли HTTPS-проверку на реальном расписании: предметы всего дня, предмет конкретной пары, уточнение в открытом разговоре и предметы преподавателя. Проверочные запросы использовали отдельные тестовые состояния: настройки владельца в хранилище Яндекса не сбрасывались и не менялись. Полные ответы закрывали сессию при включённом автовыходе. Звучание новых фраз на Станции не проверено.

Контейнер healthy, `/readyz` — ready. SHA-256 изменённых файлов совпал с локальным; контрольные суммы проверенных файлов бота и virtual host не изменились. Перед обновлением сохранены образ `alisa-schedule:before-subject-clarifications-20260916` и `/opt/alisa-schedule/before-subject-clarifications-20260916.tar.gz`. При откате код и `data/responses.json` восстанавливаются вместе. Новых зависимостей нет.

## Запросы групп и преподавателей — 15 сентября 2026

Развёрнуты разовые запросы другой группы, расписание преподавателя, первая пара и количество пар преподавателя. Собственная группа сохраняется. Фамилия уточняется при неоднозначности; падежные формы и настроенные варианты имени распознаются. Команды «оставайся в навыке» и «выходи после ответа» сохраняют личную настройку автовыхода.

В проекте 154 теста. Полный набор из 144 тестов прошёл до последних уточнений; после добавления ещё 10 проверок прошли 68 тестов адресатов и 49 тестов прежних диалогов и ответов. Проверены замены преподавателей, совмещённые пары, неполные данные, точная дата, кэш, `404`, неверные инициалы, сохранение исходного вопроса при уточнении и окончание последней страницы. Lint, форматирование и проверка данных прошли. Новых зависимостей нет.

Адаптер проверен на трёх реальных ответах расписания преподавателя. После развёртывания оба ID навыка прошли HTTPS-сценарии: своя группа → другая группа без изменения сохранённой → преподаватель → количество → первая пара → отключение и включение автовыхода. Все завершённые ответы закрыли сессию при включённой настройке. В опубликованном тестовом чате Яндекса запрос группы 141 вернул расписание и «Сессия завершена»; следующий запрос «моя группа» подтвердил сохранённую 103-Д9-3ИНС.

Контейнер healthy, `/readyz` — ready. Проверенные файлы приложения совпали с локальными по SHA-256. Контрольные суммы `api.py`, `schedules/group_schedule.py` и virtual host существующего ККЭПика не изменились; сайт и документация API отвечают HTTP 200.

Резервная копия перед обновлением: образ `alisa-schedule:before-teacher-schedules-20260915` и `/opt/alisa-schedule/before-teacher-schedules-20260915.tar.gz`. Ответы и код нужно откатывать вместе: в этой версии добавлены шаблоны реплик.

Звучание новых запросов и их распознавание непосредственно на Станции ещё не проверены. Тестовый навык уже использует обновлённый backend; публичная карточка получает те же изменения через общий webhook.

## Backend

Сервис работает в `/opt/alisa-schedule`, контейнер `alisa-schedule-skill-1` имеет статус healthy. `/readyz` возвращает ready. Webhook: `https://kkepik.rub1kub.ru/alice/webhook`.

До подключения приватного навыка прошли все 70 тестов. Проверены обязательные запросы, даты и МСК, выбор группы, память пользователя и гостя, замены, отмены, кэш, объединение запросов, 404, 429 и таймауты. Проверки ответов охватывают режим по курсу, фамилии без инициалов, отсутствие кабинетов и даты, сохранение формата, явный запрос предметов, валидацию шаблонов и продолжение длинного ответа.

Для приватного навыка добавлены 5 проверок: настройка второго ID через окружение, обязательность основного ID в production, допуск только настроенных ID и отсутствие переноса группы между навыками. После правки прошёл набор из 11 релевантных тестов; полный набор повторно не запускался. Через HTTPS оба настоящих ID прошли выбор группы, завтра, первую пару и количество. Неизвестный ID получил 403. Контейнер healthy, `/readyz` возвращает ready. В чате Яндекса без экрана выбранная группа сохранилась, на «сколько пар завтра» получен ответ «3 пары.».

### Автоматический выход

Включён `options.auto_exit`. Полный ответ о расписании завершает сессию, уточнения и промежуточные страницы — нет. Группа и формат сохраняются после выхода. Отсутствие опубликованного расписания и недоступность источника также завершают разговор.

Прошли 49 релевантных тестов диалога и ответов, включая 11 новых проверок выхода. Они покрывают повторный запуск для гостя и пользователя, сохранение формата, ошибки источника, уточнения, последнюю страницу и отключение автоматического выхода. Два теста повторно проверены после перевода на новые сессии; форматирование, lint и валидация данных прошли.

После развёртывания оба ID прошли HTTPS-проверку: выбор группы оставил сессию открытой, расписание, первая пара и количество завершили её. Следующие запросы начинались с новой сессии и сохранённой группой. В чате опубликованного тестового навыка без экрана ответ «3 пары.» пришёл с `end_session: true`; консоль показала «Сессия завершена». Контейнер healthy, `/readyz` возвращает ready. Контрольные суммы обновлённых файлов совпадают с локальными; проверенные файлы бота и virtual host не изменились.

`ruff check`, `ruff format --check`, проверка JSON и сборка Docker прошли. У тестового клиента остаются два предупреждения об устаревающих интерфейсах Starlette/httpx и AnyIO; runtime-ошибок не было.

## Реальные запросы

- Через HTTPS проверены выбор группы, завтра, первая пара и количество.
- На расписании 16 сентября проверены группы первого, второго, третьего и четвёртого курсов. Первый курс получает предметы, старшие — фамилии. Поля преподавателей в проверенных данных содержат фамилию и инициалы; инициалы удаляются.
- После отключения даты ответы для `141-Д9-1КСК` и `103-Д9-3ИНС` занимают 41 и 56 символов. День сохраняется в состоянии диалога, но не проговаривается.
- В тестовом чате Яндекса команда «называй предметы» изменила ответ; выбор сохранился после «выход» и нового обращения. После теста возвращён режим по курсу.
- Яндекс преобразует «какая первая пара» в «какая 1 пара». Этот вариант проверен в чате и отдельным тестом.
- В каталоге источника 65 доступных групп после исключения ошибочной `103-Д3-3ИНС` только в навыке.
- Существующие `/` и `/api/docs/` отвечают HTTP 200. Контрольные суммы virtual host, `api.py` и `schedules/group_schedule.py` бота совпадают с исходными.

При первоначальном развёртывании проверен тёплый кэш: 64 успешных запроса, concurrency 8, медиана 236,4 мс, p95 297,7 мс. Измерение выполнено из контейнера с лимитом 0,5 CPU и 192 MiB памяти; внешний TLS и холодные запросы к источнику в него не входят. При обновлении только текстов этот замер не повторялся.

## Публикация и ограничения

Карточка «ККЭПик» повторно отправлена на модерацию с голосом «Алиса» и тремя дополнительными именами: «Кэпик», «ККЭПика», «Кэпика». Для однословного имени использован уже добавленный в Вебмастер основной домен `rub1kub.ru`; webhook остался на `kkepik.rub1kub.ru`. Пример запуска: «Алиса, спроси у ККЭПика, что завтра». Реквизиты записаны в `deploy/skill-card.json`. Доступ в каталоге зависит от одобрения и последующей публикации.

Пользователь подтвердил запуск приватного навыка с вариантом «Кэпик тест». Для тестовой версии также выбран голос «Алиса». Смена голоса и активационных имён относилась только к карточкам навыков. Для неё проверены JSON-файлы карточек и сохранение настроек в консоли; тесты backend тогда повторно не запускались.

Проверены HTTP-сервис и текстовый чат Диалогов. Запуск публичного навыка, команда через домашний сценарий и звучание после смены голоса ещё не подтверждены на Станции. Финальная модерация и длительная массовая нагрузка также остаются за пределами этих проверок. При смене формата API потребуется обновление адаптера.
